[package]
name = "denon-avr-remote"
version = "0.1.0"
edition = "2021"
description = "Denon and Marantz AVR IP control library"
license = "MIT"

[lib]
name = "denon_avr_remote"
path = "src/lib.rs"

[dependencies]
if-addrs = "0.15"
